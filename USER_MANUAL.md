# 小马办公 - 用户手册

> 本手册详细介绍了 PonyWork（小马办公）的所有功能使用方法。

---

## 目录

- [简介](#简介)
- [快速入门](#快速入门)
- [应用管理](#应用管理)
- [集合管理](#集合管理)
- [AI 智能功能](#ai-智能功能)
- [远程桌面](#远程桌面)
- [备忘录](#备忘录)
- [定时任务](#定时任务)
- [设置中心](#设置中心)
- [系统托盘](#系统托盘)
- [常见问题](#常见问题)

---

## 简介

### 关于 PonyWork

PonyWork（小马办公）是一款基于 Qt 5.15.2 开发的 Windows 桌面办公助手应用，旨在帮助用户更高效地管理和使用电脑。

### 主要功能一览

| 功能模块 | 功能数量 | 说明 |
|---------|---------|------|
| 应用管理 | 10+ | 应用添加、编辑、删除、启动、批量操作 |
| 集合管理 | 5+ | 分组管理、批量启动 |
| AI 智能 | 9种模型 | AI图标生成、智能对话 |
| 远程桌面 | 3种模式 | FRP远程连接、会话管理 |
| 备忘录 | 完整CRUD | 创建、编辑、删除、同步 |
| 定时任务 | 4种 | 关机、重启、休眠、取消 |
| 底部快捷栏 | 5+ | 快速启动、拖拽排序 |

### 版本信息

- **当前版本**: v1.0.5
- **发布日期**: 2026-03-30

---

## 快速入门

### 首次运行

1. 下载并解压 `PonyWork-v*x.x.x-win64.zip`
2. 双击 `PonyWork.exe` 运行程序
3. 程序主窗口将显示在桌面上

### 主界面介绍

```
┌─────────────────────────────────────────────────────────┐
│  [图标] 小马办公                    [─] [□] [×]        │  <- 标题栏
├─────────────────────────────────────────────────────────┤
│  [应用管理] [集合管理] [AI图标] [远程桌面] [备忘录]      │  <- 标签栏
│  [定时关机] [工作日志] [设置]                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    功能区域                             │  <- 主内容区
│                                                         │
├─────────────────────────────────────────────────────────┤
│  [快捷应用1] [快捷应用2] [快捷应用3] ...                │  <- 底部快捷栏
└─────────────────────────────────────────────────────────┘
```

---

## 应用管理

### 打开应用管理

点击标签栏中的「**应用管理**」标签，进入应用管理界面。

### 添加应用

**步骤：**

1. 点击右上角「**添加应用**」按钮
2. 在弹出的文件选择对话框中，浏览并选择 `.exe` 可执行文件
3. 系统会自动提取应用图标和名称
4. 点击「**确定**」保存

**说明：**
- 支持拖拽文件到窗口直接添加
- 自动从可执行文件中提取图标
- 可手动编辑应用名称和描述

### 编辑应用

**步骤：**

1. 在应用列表中，选中要编辑的应用
2. 点击右上角「**编辑应用**」按钮
3. 在弹出的对话框中修改应用信息
4. 点击「**确定**」保存

**可编辑信息：**
- 应用名称
- 应用图标（可使用 AI 生成新图标）
- 应用路径
- 描述信息

### 删除应用

**步骤：**

1. 在应用列表中，选中要删除的应用（支持多选）
2. 点击右上角「**删除应用**」按钮
3. 在确认对话框中点击「**确定**」

**多选方式：**
- `Shift + 点击`：连续选择
- `Ctrl + 点击`：逐个选择
- `鼠标拖拽`：框选

### 启动应用

**方式一：双击启动**
- 在应用列表中，双击应用图标即可启动

**方式二：按钮启动**
1. 选中应用
2. 点击右上角「**启动应用**」按钮

### 批量操作

#### 批量启动

1. 使用多选方式选中多个应用
2. 点击「**批量启动**」按钮
3. 所有选中的应用将依次启动

#### 批量删除

1. 使用多选方式选中多个应用
2. 点击「**删除应用**」按钮
3. 在确认对话框中点击「**确定**」

### 视图模式

点击右下角视图切换按钮，可在以下模式间切换：

| 模式 | 说明 |
|------|------|
| 大图标 | 以大图标形式显示应用，适合触控操作 |
| 列表 | 以列表形式显示应用，显示更多信息 |

### 排序与收藏

#### 排序

点击列表顶部的排序选项，可按以下方式排序：
- 按名称（A-Z / Z-A）
- 按添加时间

#### 收藏

1. 右键点击应用
2. 选择「**添加到收藏**」
3. 收藏的应用将显示在收藏夹中

---

## 集合管理

### 打开集合管理

点击标签栏中的「**集合管理**」标签，进入集合管理界面。

### 创建集合

**步骤：**

1. 点击右上角「**添加集合**」按钮
2. 在弹出的对话框中输入集合名称
3. 从应用列表中选择要包含的应用（可多选）
4. 点击「**确定**」保存

### 编辑集合

**步骤：**

1. 在集合列表中，选中要编辑的集合
2. 点击右上角「**编辑集合**」按钮
3. 修改集合名称或应用列表
4. 点击「**确定**」保存

### 删除集合

**步骤：**

1. 在集合列表中，选中要删除的集合
2. 点击右上角「**删除集合**」按钮
3. 在确认对话框中点击「**确定**」

### 批量启动集合

**步骤：**

1. 在集合列表中，选中要启动的集合
2. 点击「**启动集合**」按钮
3. 集合中的所有应用将依次启动

### 集合排序

1. 点击集合列表上方的「**排序**」按钮
2. 选择排序方式（按名称/按添加时间）
3. 集合列表将重新排序

---

## AI 智能功能

### 打开 AI 图标

点击标签栏中的「**AI图标**」标签，进入 AI 图标生成界面。

### 配置 AI

首次使用需要配置 AI 模型：

**步骤：**

1. 点击右上角「**设置**」按钮
2. 在「AI 设置」中选择要使用的模型
3. 输入 API Key
4. （可选）自定义 API 地址
5. 点击「**测试连接**」验证配置
6. 点击「**确定**」保存

### 支持的 AI 模型

| 模型 | 提供商 | 特点 |
|------|--------|------|
| MiniMax | MiniMax | 高性价比 |
| GPT-3.5 | OpenAI | 通用能力强 |
| GPT-4 | OpenAI | 最强对话能力 |
| Claude-3 | Anthropic | 安全可靠 |
| Gemini | Google | 多模态支持 |
| 通义千问 | 阿里云 | 中文优化 |
| 讯飞星火 | 讯飞 | 语音友好 |
| DeepSeek | 硅基流动 | 国产优选 |
| Flux | BlackForest | 图像生成 |

### 生成图标

**步骤：**

1. 输入图标描述（英文效果更佳）
2. 点击「**生成**」按钮
3. 等待 AI 生成完成
4. 选择满意的图标并点击「**应用**」

**提示：**
- 描述越详细，生成效果越好
- 可以多次生成直到满意
- 生成的图标会自动保存到应用

### AI 对话测试

1. 点击右上角「**对话测试**」按钮
2. 在对话框中输入问题
3. 点击「**发送**」获取 AI 回复

---

## 远程桌面

### 打开远程桌面

点击标签栏中的「**远程桌面**」标签，进入远程桌面管理界面。

### 功能区域

```
┌─────────────────────────────────────────────────────────┐
│  本机 FRPC 中继                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 状态: 未启动                                     │   │
│  │ 端口: --                                        │   │
│  │                    [一键设置] [开启] [停止]      │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  远程桌面列表                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 设备名称    │ 状态    │ 操作                    │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ 我的电脑    │ 在线    │ [连接] [编辑] [删除]    │   │
│  └─────────────────────────────────────────────────┘   │
│                              [+ 添加到列表]             │
└─────────────────────────────────────────────────────────┘
```

### 启用本机远程

**步骤：**

1. 确保本机已启用「远程桌面」功能：
   - 打开「设置」→「系统」→「远程桌面」
   - 启用「启用远程桌面」
2. 在 PonyWork 中点击「**一键设置**」
3. 等待 FRPC 启动成功
4. 记录显示的端口号
5. 点击「**添加到列表**」将本机添加

### 连接远程桌面

**步骤：**

1. 在远程桌面列表中，双击要连接的设备
2. 系统会打开 Windows 远程桌面连接
3. 输入目标电脑的用户名和密码
4. 点击「**连接**」

### 管理远程设备

#### 编辑设备

1. 选中设备，点击「**编辑**」
2. 修改设备名称或连接信息
3. 点击「**确定**」保存

#### 删除设备

1. 选中设备，点击「**删除**」
2. 在确认对话框中点击「**确定**」

### 远程桌面原理

```
┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│   本机       │          │   FRP服务器   │          │   远程电脑   │
│  (被控制端)   │◄────────►│  (中转)      │◄────────►│  (控制端)    │
│   frpc      │          │   frps       │          │   RDP       │
│  端口:3389   │          │  端口:7000   │          │             │
└──────────────┘          └──────────────┘          └──────────────┘
```

FRP（Fast Reverse Proxy）通过公网服务器中转，实现内网穿透，无需公网IP即可远程连接。

---

## 备忘录

### 打开备忘录

点击标签栏中的「**备忘录**」标签，进入备忘录管理界面。

### 功能布局

```
┌─────────────────────────────────────────────────────────┐
│  备忘录列表                    [+ 新建] [同步] [刷新]   │
│  ┌─────────────┐  ┌─────────────────────────────────┐   │
│  │ 工作计划     │  │ 标题: 工作计划                  │   │
│  │ 学习笔记     │  │ 类型: 普通                      │   │
│  │ 脚本收藏     │  │ 描述: 今日待办                  │   │
│  │             │  │ ────────────────────────────── │   │
│  │             │  │ [内容区域 - 分段显示]           │   │
│  │             │  │                               │   │
│  │             │  │ [复制] [执行] [编辑] [删除]    │   │
│  └─────────────┘  └─────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 创建备忘录

**步骤：**

1. 点击「**新建**」按钮
2. 在弹出的对话框中填写：
   - 名称：备忘录标题
   - 类型：普通 / 命令 / 脚本
   - 内容：备忘录正文
   - 描述：简短说明
3. 点击「**确定**」保存

### 编辑备忘录

**步骤：**

1. 在备忘录列表中，选中要编辑的备忘录
2. 点击「**编辑**」按钮
3. 修改备忘录内容
4. 点击「**确定**」保存

### 删除备忘录

**步骤：**

1. 在备忘录列表中，选中要删除的备忘录
2. 点击「**删除**」按钮
3. 在确认对话框中点击「**确定**」

### 备忘录类型

| 类型 | 说明 | 特殊功能 |
|------|------|---------|
| 普通 | 普通文本备忘录 | 复制内容 |
| 命令 | PowerShell 命令 | 可执行命令 |
| 脚本 | 脚本内容 | 可运行脚本 |

### 执行命令/脚本

对于类型为「命令」或「脚本」的备忘录：

1. 选中备忘录
2. 点击「**执行**」按钮
3. 命令将在 PowerShell 中执行

### 同步备忘录

备忘录支持云端同步：

1. 确保已登录账号
2. 点击「**同步**」按钮
3. 等待同步完成

### 右键菜单

在备忘录列表或内容区域右键，可使用以下功能：

- 复制内容
- 执行命令/脚本
- 编辑
- 删除

---

## 定时任务

### 打开定时关机

点击标签栏中的「**定时关机**」标签，进入定时任务管理界面。

### 功能布局

```
┌─────────────────────────────────────────────────────────┐
│  定时任务                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  操作类型:  ○ 关机  ○ 重启  ○ 休眠               │   │
│  │                                                   │   │
│  │  定时方式:  ○ 指定时间  ○ 快捷时间                │   │
│  │                                                   │   │
│  │  时间选择:  [10:30]              [开始] [取消]    │   │
│  │                                                   │   │
│  │  快捷时间:  [30分钟后] [1小时后] [2小时后]       │   │
│  │            [今天午夜] [明天早上]                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                       │
│  当前状态: 未设置定时任务                               │
└─────────────────────────────────────────────────────────┘
```

### 设置定时任务

**方式一：指定时间**

1. 选择操作类型（关机/重启/休眠）
2. 选择「指定时间」
3. 设置具体时间
4. 点击「**开始**」

**方式二：快捷时间**

1. 选择操作类型（关机/重启/休眠）
2. 选择「快捷时间」
3. 点击预设的时间按钮（如「1小时后」）
4. 系统将自动计算并设置

### 取消定时任务

1. 点击「**取消**」按钮
2. 定时任务将被取消

### 操作类型说明

| 操作 | 说明 |
|------|------|
| 关机 | 关闭计算机，所有程序将关闭 |
| 重启 | 关闭计算机后自动重新启动 |
| 休眠 | 计算机进入休眠状态，内存数据保存 |

### 快捷时间选项

| 选项 | 说明 |
|------|------|
| 30分钟后 | 30分钟后执行 |
| 1小时后 | 1小时后执行 |
| 2小时后 | 2小时后执行 |
| 今天午夜 | 今天晚上 23:59 执行 |
| 明天早上 | 明天早上 08:00 执行 |

---

## 设置中心

### 打开设置

点击标签栏中的「**设置**」标签，进入设置中心。

### 设置分类

#### 1. 通用设置

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| 开机自动启动 | 开机时自动运行程序 | 关闭 |
| 最小化到系统托盘 | 关闭窗口时最小化到托盘 | 开启 |
| 关闭提示 | 关闭时显示确认提示 | 开启 |

**步骤：**

1. 在设置列表中点击「通用设置」
2. 勾选或取消勾选相应选项
3. 设置自动保存

#### 2. 快捷键设置

1. 在设置列表中点击「快捷键设置」
2. 点击要设置快捷键的功能
3. 按下要设置的键位组合
4. 设置自动保存

#### 3. AI 设置

详见 [AI 智能功能](#ai-智能功能)

#### 4. 检查更新

1. 在设置列表中点击「检查更新」
2. 点击「检查更新」按钮
3. 如有新版本，按照提示下载更新

#### 5. 关于

显示以下信息：
- 软件版本
- Qt 版本
- 开发者信息
- 许可证信息

### 界面预览

```
┌─────────────────────────────────────────────────────────┐
│  设置                                                 │
│  ┌───────────────┐ ┌───────────────────────────────┐   │
│  │ ▸ 通用设置     │ │  通用设置                    │   │
│  │ ▸ 快捷键设置   │ │                              │   │
│  │ ▸ AI 设置     │ │  [✓] 开机自动启动            │   │
│  │ ▸ 检查更新     │ │  [✓] 最小化到系统托盘        │   │
│  │ ▸ 关于        │ │  [✓] 关闭提示               │   │
│  │               │ │                              │   │
│  └───────────────┘ └───────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 系统托盘

### 托盘图标

程序运行后，会在系统托盘区域显示图标。

### 托盘菜单

**右键点击托盘图标**，弹出菜单：

| 菜单项 | 说明 |
|--------|------|
| 打开窗口 | 显示主窗口 |
| 退出程序 | 完全退出程序 |

### 双击恢复

**双击托盘图标**可快速恢复主窗口显示。

### 最小化到托盘

当「设置」→「通用设置」→「最小化到系统托盘」开启时：
- 点击窗口关闭按钮，窗口会隐藏到托盘
- 程序继续在后台运行

### 完全退出

1. 右键点击托盘图标
2. 选择「退出程序」
3. 程序将完全退出

---

## 常见问题

### 安装运行问题

#### Q: 提示 "无法启动此程序，因为计算机中丢失 xxx.dll"

**A:** 这是 Qt 运行库缺失的问题。请安装 Visual C++ Redistributable：

1. 下载 [Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)
2. 安装后重新运行程序

#### Q: 提示 "不是有效的 Win32 应用程序"

**A:** 请确认：
1. 下载的是 64 位版本（文件名包含 win64）
2. 操作系统是 64 位 Windows

### 应用管理问题

#### Q: 添加的应用图标显示不正确

**A:** 可以尝试以下方法：
1. 手动编辑应用，指定自定义图标
2. 使用 AI 图标生成功能生成新图标
3. 某些绿色软件可能无法提取图标

#### Q: 应用启动失败

**A:** 请检查：
1. 应用路径是否正确
2. 应用是否移动或删除
3. 是否有管理员权限要求

### AI 功能问题

#### Q: AI 功能无法使用

**A:** 请按以下步骤排查：
1. 确认已配置正确的 API Key
2. 检查网络连接
3. 部分 AI 模型（如 OpenAI、Claude）需要代理才能访问
4. 建议使用国内 AI 服务（通义千问、DeepSeek 等）

#### Q: API Key 安全吗？

**A:** 您的 API Key 采用加密存储，仅存储在本地，不会传输到非必要服务器。

### 远程桌面问题

#### Q: FRPC 启动失败

**A:** 请检查：
1. 防火墙是否阻止了程序
2. 服务器地址和端口是否正确
3. 网络连接是否正常

#### Q: 远程连接不上

**A:** 请检查：
1. 目标电脑是否开启远程桌面
2. 用户名密码是否正确
3. 网络是否可达
4. 端口是否被防火墙阻止

### 数据同步问题

#### Q: 同步失败

**A:** 请检查：
1. 是否已登录账号
2. 网络连接是否正常
3. 服务器是否可用

#### Q: 数据冲突怎么办

**A:** 系统会提示冲突，您可以选择：
- 使用本地数据
- 使用云端数据
- 手动合并

---

## 附录

### 数据存储位置

| 数据类型 | 路径 |
|---------|------|
| 本地数据 | `%APPDATA%\PonyWork\data.json` |
| 日志文件 | `%APPDATA%\PonyWork\logs\` |
| 应用缓存 | `%APPDATA%\PonyWork\cache\` |

### 快捷键

| 功能 | 快捷键 |
|------|--------|
| 打开主窗口 | （托盘双击）|
| 退出程序 | （托盘右键→退出）|

### 联系我们

- **GitHub**: [提交问题](https://github.com/jiasu1017-beep/PonyWork/issues)
- **微信公众号**: 梁柱墙笔记

---

**© 2024-2026 小马办公. All rights reserved.**
